{% extends 'base.html.twig' %}

{% block title %}Admin !{% endblock %}

{% block body %}

{% for flash in app.flashes('success') %}
    {{ flash }}
{% endfor %}

<table class="table">
  <thead>
    <tr>
      <th scope="col">#</th>
      <th scope="col">Title</th>
      <th scope="col">Date de création</th>
      <th scope="col">Catégorie(s)</th>
      <th scope="col">Url Image</th>
      <th scope="col">User</th>
      <th></th>
      <th></th>
    </tr>
  </thead>
  <tbody>
    {% for post in posts %}
        <tr>
            <th scope="row">{{post.id}}</th>
            <td>{{post.title}}</td>
            <td>{{post.dateCreate|date('d/M/Y')}}</td>
            <td>
                {% for category in post.categories %}
                   {{ category.title }} -
                {% endfor %}
            </td>
            <td>{{ post.image }}</td>
            <td>{{ post.user.fullname }}</td>
            <td><a href="{{ path('app_post_update', {'id' : post.id}) }}">Modifier</a></td>
            <td><a href="{{ path('app_post_remove', {'id' : post.id}) }}">Supprimer</a></td>
        </tr>
    {% endfor %}
  </tbody>
</table>

{% endblock %}
